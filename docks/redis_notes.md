# Redis 정리

## Redis란?

**Redis(REmote DIctionary Server)**는 오픈소스 **인메모리 자료구조 저장소(In-Memory Data Structure Store)**다. 데이터를 디스크가 아닌 RAM에 저장해 빠른 읽기/쓰기 속도를 제공하며, 데이터베이스·캐시·메시지 브로커·세션 스토어 등 다양한 용도로 쓰인다.

단순 Key-Value 저장소가 아니라, 값 자체가 String, List, Hash, Set, Sorted Set(ZSet), Stream, Bitmap, HyperLogLog, Geo 등 다양한 **자료구조** 타입을 가진다는 점이 특징이다. 스키마가 고정되어 있지 않은 **스키마리스(schema-less) NoSQL(비관계형) 저장소**다.

---

## 1. 프로세스 구조

Redis는 `redis-server`라는 프로그램이 실행되어 만들어진 하나의 프로세스다.

- **명령 실행은 싱글 스레드**로 동작한다. 이벤트 루프가 클라이언트 요청을 큐잉해 하나씩 순차 처리하며, 이 덕분에 명령 하나하나는 원자적으로 실행되고 락 경합이 없다.
- 다만 프로세스 전체가 하나의 스레드로만 도는 것은 아니다.
  - RDB/AOF 저장 시 `fork()`로 자식 프로세스를 만든다.
  - 4.0부터 lazy free, fsync 등을 처리하는 백그라운드 스레드가 있다. 

  lazy free = 논리적 삭제... 삭제하는데 비용이 많이 들 경우 unlink 상태로 참조할 수 없게만 만드는 것. 


   실제로 삭제가 아닌 백그라운드로 보내고 unlink (비동기 삭제)
  - 6.0부터 네트워크 I/O를 병렬화하는 I/O 스레드가 있다.

## 2. 데이터 모델

- **인메모리 데이터베이스**: 데이터를 RAM에 저장해 디스크 기반 DB보다 훨씬 빠르다.
- **NoSQL(비관계형)**: 테이블/조인 기반이 아니라 Key-Value 구조이며, 값이 다양한 자료구조(String, List, Hash, Set, ZSet 등)를 가질 수 있는 **자료구조 서버(Data Structure Server)**다.
- **RAM 용량 한계**: 단일 노드에서는 저장 가능한 데이터 크기가 물리 RAM을 넘을 수 없다. 포인터·메타데이터·메모리 단편화·복제 버퍼·fork 시 Copy-on-Write 등으로 실제 사용량은 원본 데이터 크기보다 커진다. 이 한계는 **Redis Cluster**로 데이터를 여러 노드에 샤딩해 확장할 수 있다.

샤딩: 하나의 데이터셋을 특정 기준에 따라 여러개로 나누는 것
즉, ram 용량 한계를 해결하기 위해서는 여러 컴퓨터를 연결하고 redis를 여러 개로 쪼갠다. 
물리적으로 해결할 수 없으니 ram을 여러개 사용하자는 뜻

## 3. 메모리 관리 (Eviction)

- `maxmemory`를 설정하지 않으면 메모리 사용량이 계속 늘어나 OS가 스왑을 유발할 수 있어 성능에 치명적이다. 실무에서는 `maxmemory`를 설정하고 THP 비활성화, swappiness 조정 등을 함께 한다.
- 메모리가 가득 찼을 때의 동작은 **eviction 정책**으로 결정되며, 기본값은 `noeviction`(쓰기 명령에 에러 반환)이다. LRU/LFU 기반 정리는 명시적으로 정책을 설정해야 동작한다.
- Redis의 LRU/LFU는 전수 조사가 아니라 **샘플링 기반 근사 알고리즘**이다(`maxmemory-samples`, 기본 5개를 뽑아 그중 가장 오래되었거나 덜 쓰인 키를 제거). LFU는 4.0부터 지원.
- 정책 종류: `noeviction` / `allkeys-lru` / `allkeys-lfu` / `allkeys-random` / `volatile-lru` / `volatile-lfu` / `volatile-random` / `volatile-ttl`
  - `volatile-*`는 TTL이 설정된 키만 삭제 후보로 삼는다.

## 4. 영속성 (Persistence)

Redis는 인메모리 DB지만 디스크에 데이터를 남겨 재시작 시 복구할 수 있는 영속성 메커니즘을 제공한다.

- **RDB (Redis DataBase)**: 특정 시점의 전체 데이터를 스냅샷으로 저장. `fork()`로 자식 프로세스를 만들고 Copy-on-Write 방식으로 덤프 파일을 기록한다.
- **AOF (Append Only File)**: 실행된 쓰기 명령 자체를 로그로 순서대로 append. 파일이 커지면 AOF Rewrite로 압축한다(이 역시 fork 사용).
- fsync 정책: `always`(매 쓰기마다 동기화, 느리지만 안전) / `everysec`(기본값, 최대 1초치 유실 가능) / `no`(OS에 위임).
- 두 방식 모두 일정 수준의 데이터 유실 가능성이 있는 **best-effort 영속성**이며, RDBMS 수준의 내구성(durability)을 보장하지는 않는다.
- 저장소 용도로 쓸 때는 RDB와 AOF를 **함께 사용**하는 것이 일반적이며, 재시작 복구 시 AOF가 활성화되어 있으면 AOF가 우선 사용된다.

## 5. 동시성과 원자성

- 싱글 스레드 명령 실행 덕분에 **명령 하나**는 항상 원자적이다.
- 하지만 `GET → 계산 → SET`처럼 여러 명령에 걸친 로직은 그 사이에 다른 클라이언트의 명령이 끼어들 수 있어 애플리케이션 레벨의 경쟁 상태가 발생할 수 있다.
- 해결 수단:
  - `INCR` 등 원자 연산 명령 사용
  - `MULTI`/`EXEC`로 여러 명령을 묶어 트랜잭션처럼 실행 (단, 롤백은 지원하지 않는다 — 중간 명령이 실패해도 나머지 명령은 그대로 실행됨)
  - `WATCH`를 이용한 낙관적 락(optimistic locking)
  - Lua 스크립트로 여러 로직을 원자적으로 실행
- 무거운 명령 하나가 싱글 스레드 전체를 블로킹할 수 있다(Head-of-Line Blocking)는 점은 유의해야 한다.

동시성: 여러 클라이언트가 요청을 보낼 때 처리하고 관리하는 개념 (줄세우기- 단일 스레드 )
원자성: 명령어가 완전히 실행되거나 아예 실행되지 않거나 둘 중 하나인 것ㄱ (중간에 끼어들기 X)

## 6. 활용 사례

Redis는 여러 애플리케이션 서버가 공유하는 데이터 저장소로 자주 쓰인다.

- 세션 스토어, 분산 락, 공유 캐시, 처리율 제한(Rate Limiting), 랭킹보드(리더보드) 등
- 여러 서버가 하나의 Redis에 의존하면 Redis 자체가 **단일 장애점(SPOF)**이 될 수 있어, 실무에서는 복제(Replication)와 Sentinel/Cluster를 함께 구성해 가용성을 확보한다.

## 7. 성능 (속도)

Redis의 가장 큰 장점은 속도이며, 그 이유는 다음과 같다.

1. 데이터를 메모리(RAM)에서 다룬다.
2. 자료구조가 단순하고 처리에 최적화되어 있다.
3. 명령 실행이 싱글 스레드라 락/컨텍스트 스위칭 비용이 없다.
4. 이벤트 루프 기반 I/O 멀티플렉싱으로 다수의 연결을 효율적으로 처리한다.

단, 실제 서비스에서는 네트워크 왕복 시간(RTT)이 병목이 되는 경우가 많아, 여러 명령을 한 번에 보내 왕복 횟수를 줄이는 **파이프라이닝(Pipelining)**이 실무에서 중요하다.

## 8. 캐싱 전략

- **Cache-Aside (Look-aside)**: 애플리케이션이 캐시를 먼저 조회하고, 없으면(캐시 미스) 원본 저장소(DB)에서 조회한 뒤 캐시에 적재하는 가장 일반적인 패턴.
- 함께 알아둘 다른 전략: Read-Through, Write-Through, Write-Behind
- 함께 알아둘 문제:
  - **캐시 스탬피드(Cache Stampede)**: 다수의 키가 동시에 TTL 만료되어 DB에 요청이 몰리는 현상
  - **정합성/무효화(Invalidation)**: 원본 데이터 변경 시 캐시를 갱신하거나 무효화하는 문제

## 9. 캐시 계층에서의 위치

Redis는 CPU 캐시와 달리 **소프트웨어 캐시**로, 개발자가 무엇을·언제·얼마나 캐싱할지 명시적으로 제어할 수 있다는 점에서 CPU 캐시(하드웨어가 자동·투명하게 관리)와 다르다.

다만 Redis는 로컬 프로세스 메모리(RAM) 접근보다는 느리다. 네트워크 통신과 직렬화 비용이 추가되기 때문이다.

대략적인 접근 속도 계층:

```
CPU 캐시(ns) < 로컬 RAM(수십 ns) < Redis(수십 µs ~ ms, 네트워크 포함) < 디스크/DB(ms)
```

즉 Redis가 빠른 이유는 "RAM이라서"가 아니라, **디스크 기반 DB 대비 빠르기 때문**이라고 이해하는 것이 정확하다.
