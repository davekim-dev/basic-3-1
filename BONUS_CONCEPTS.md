# 보너스 과제 — basic-3-1에 없는 개념 정리

## 0. 먼저: 지금까지 구현한 것 vs 보너스 과제 5개

basic-3-1(GUIDE.md)은 아래 3개 자료구조로 Mini Redis의 LRU + TTL을 구현했다.

| 이미 구현됨 | 핵심 특성 |
|---|---|
| 이중 연결 리스트 (`doubly_linked_list.py`) | 포인터 기반, 양 끝 삽입/삭제 O(1), **임의 위치도** `remove_node`로 O(1) (위치를 이미 알고 있을 때) |
| 해시맵 체이닝 (`hash_map.py`) | 버킷 배열 + DLL 체이닝, 로드 팩터 0.75 초과 시 배열 2배 확장 + 재해싱 |
| 최소 힙 (`min_heap.py`) | 배열로 완전 이진 트리 표현, 부모/자식은 인덱스 산술로만 계산, `push`/`pop` O(log n) |

보너스 과제로 추가된 5개 중, **이미 다룬 개념과 겹치는 부분**과 **완전히 새로운 개념**을 구분하면:

| 보너스 과제 | 겹치는 부분 | basic-3-1에 없는 새 개념 |
|---|---|---|
| 1. 동적 배열 직접 구현 | 해시맵의 "버킷 배열 2배 확장"이 개념적으로 동일 | **배열 자체를 capacity/size 분리해서 직접 구현**하는 것 (지금까지는 Python `list`에 위임) |
| 2. 스택/큐/덱 | DLL의 양 끝 삽입/삭제 재사용 가능 | **스택(LIFO)/큐(FIFO)라는 "접근 정책" 개념 자체**, 그리고 각각의 용도 |
| 3. 이진 트리 + 순회 | 힙도 "배열로 표현한 완전 이진 트리" | **전위/중위/후위/레벨 순회 알고리즘** — 힙은 순회를 하지 않는다 |
| 4. 이진 탐색 트리(BST) | 트리 구조라는 점은 힙과 유사 | **좌 < 부모 < 우 정렬 불변식**, 이걸로 하는 탐색/삽입/삭제 — 힙의 정렬 불변식과 다름 |
| 5. Pub/Sub | 채널 저장에 해시맵, 큐잉에 DLL 재사용 가능 | **채널 기반 메시징(발행-구독) 모델 자체** — 지금까지는 key-value 저장/조회만 다룸 |

아래에서 "새 개념"만 정의하고 설명한다.

---

## 1. 동적 배열 (Dynamic Array)

### 정의
크기가 고정된 저수준 배열을 내부에 두고, 사용자에게는 "길이가 자유롭게 늘어나는 배열"처럼 보이게 감싼 자료구조. `size`(실제 원소 수)와 `capacity`(내부 배열의 크기)를 분리해서 관리하는 것이 핵심이다.

```
capacity = 8   (내부 배열이 실제로 가진 슬롯 수)
size     = 5   (그중 실제로 값이 채워진 개수)
```

### basic-3-1과 무엇이 다른가
지금까지 구현한 세 자료구조는 전부 **Python `list`에 크기 관리를 위임**했다.
- `min_heap.py`: `self._data = []`, `append`/`pop`으로 크기 조절 → 배열 확장 로직은 Python 내부 구현에 맡김
- `hash_map.py`: 버킷 "개수"는 직접 2배로 늘리지만, 버킷 배열 자체(`[DoublyLinkedList() for _ in range(capacity)]`)도 결국 Python `list`

동적 배열 보너스 과제는 이 "Python list가 대신 해주던 부분"을 직접 구현하라는 것이다. 즉 `list` 없이 저수준 배열(고정 크기 버퍼)만 갖고 `append`할 때 꽉 차면 새 버퍼를 만들어 복사하는 로직을 스스로 짜야 한다.

### 핵심 메서드와 시간복잡도

| 메서드 | 역할 | 시간복잡도 |
|---|---|---|
| `get(i)` | 인덱스로 값 조회 | O(1) |
| `set(i, v)` | 인덱스에 값 갱신 | O(1) |
| `append(v)` | 맨 뒤에 추가 | 상각(amortized) O(1), 확장 시 O(n) |
| `remove(i)` | 인덱스의 값 제거, 뒤 원소들을 한 칸씩 당김 | O(n) |

### capacity 2배 확장의 원리 (상각 분석)

```
size == capacity 인 상태에서 append 호출 시:
1. capacity * 2 크기의 새 배열을 만든다
2. 기존 원소를 전부 새 배열로 복사한다  ← O(n)
3. 새 원소를 추가한다
4. 내부 배열 참조를 새 배열로 교체한다
```

- 단순하게 보면 확장이 일어나는 순간은 O(n)이라 "append는 O(n) 아닌가?" 싶지만, **확장은 자주 일어나지 않는다.** capacity가 2배씩 늘어나므로, n번 append하는 동안 확장은 총 O(log n)번만 발생하고 그때의 복사 비용을 다 더해도 O(n)에 그친다. n번의 append에 총 O(n) 비용 → 평균(상각) O(1).
- 만약 매번 "1칸씩만" 늘리는 방식이었다면, 매 append마다 전체 복사가 필요해 총 비용이 O(n²)이 된다. **2배씩** 늘리는 이유가 여기에 있다.

### 왜 배열 확장이 늘 2배인가 (해시맵과의 연결)
`hash_map.py`의 `_resize()`가 버킷 수를 2배로 늘리는 것과 정확히 같은 논리다. 해시맵 리사이즈나 동적 배열이나 "꽉 차면 배로 늘리고 기존 걸 옮긴다"는 동일한 상각 분석이 적용된다. 이 관점에서 보면 동적 배열은 해시맵 리사이즈 로직의 **더 원초적인 버전**이라고 볼 수 있다.

---

## 2. 스택 / 큐 / 덱 (Stack / Queue / Deque)

### 정의
셋 다 "원소를 순서대로 담고 꺼내는" 선형 자료구조지만, **어느 쪽 끝에서 넣고 빼는지에 대한 접근 정책**이 다르다.

| 구조 | 삽입 위치 | 삭제 위치 | 정책 이름 | 비유 |
|---|---|---|---|---|
| 스택 (Stack) | 한쪽 끝(top) | 같은 쪽 끝(top) | **LIFO** (Last In First Out) | 접시 쌓기 |
| 큐 (Queue) | 한쪽 끝(rear) | 반대쪽 끝(front) | **FIFO** (First In First Out) | 줄서기 |
| 덱 (Deque, Double-Ended Queue) | 양쪽 끝 | 양쪽 끝 | 양쪽 모두 자유 | 양쪽 문이 있는 대기줄 |

### basic-3-1과 무엇이 다른가
DLL(`doubly_linked_list.py`)은 이미 양 끝 삽입/삭제를 O(1)로 지원하므로 **구현 난이도 자체는 낮다** — 사실상 덱은 지금 DLL을 그대로 노출하면 된다. 다만 basic-3-1에는 없던 것은:
1. "왜 이 접근 제한(LIFO/FIFO)이 필요한가"라는 **개념적 이해**
2. 현재 DLL API에는 **`remove_front`가 없다** (GUIDE.md 표에는 있지만 실제 `doubly_linked_list.py` 구현에는 `remove_back`, `remove_node`만 있음). 큐(FIFO)를 만들려면 `remove_node(self._head.next)`로 우회하거나 `remove_front`를 새로 추가해야 한다.

### 각 구조의 용도
- **스택**: 함수 호출 스택, 실행 취소(undo) 히스토리, 괄호 짝 검사, DFS(깊이 우선 탐색)
- **큐**: 작업 대기열(job queue), BFS(너비 우선 탐색), 프린터 인쇄 대기열처럼 "요청 순서대로 처리"해야 하는 경우
- **덱**: 스택+큐의 상위 호환. 슬라이딩 윈도우 알고리즘, 최근 사용 항목을 양쪽에서 다뤄야 하는 경우

### Mini Redis와의 연결 (STACK_QUEUE_DEQUE.md 작성 시 참고)
과제 설명에 나온 대로, **커맨드 히스토리**(CLI에서 위/아래 화살표로 이전 명령 조회)는 스택으로, **Pub/Sub의 구독자별 메시지 버퍼**는 큐로 자연스럽게 이어진다. 둘 다 지금 있는 `DoublyLinkedList`를 감싸는 얇은 래퍼 클래스로 구현 가능하다 (push/pop 메서드 이름만 정책에 맞게 제한).

---

## 3. 이진 트리와 순회 알고리즘 (Binary Tree & Traversal)

### 정의
각 노드가 최대 2개의 자식(왼쪽/오른쪽)을 갖는 트리. `min_heap.py`도 "완전 이진 트리"이지만, **일반 이진 트리는 완전할 필요가 없고, 정렬 규칙도 없다** — 그냥 부모-자식 관계만 존재하는 구조다.

### basic-3-1(Min-Heap)과 무엇이 다른가

| | Min-Heap | 일반 이진 트리 |
|---|---|---|
| 형태 제약 | 완전 이진 트리 (배열로 빈틈없이 채움) | 제약 없음 (배열 대신 노드+포인터로 표현하는 경우가 많음) |
| 정렬 규칙 | 부모 ≤ 자식 (약한 순서) | 없음 (BST에서만 강한 순서 규칙이 생김) |
| 주요 연산 | `push`/`pop` (최솟값 접근) | **순회(traversal)** — 모든 노드를 특정 순서로 방문 |
| 순회 지원 여부 | 순회하지 않음 (배열 인덱스 산술만 씀) | 순회가 핵심 연산 |

즉 힙은 "루트가 항상 최솟값"이라는 것만 보장하고 나머지 노드 간 순서는 신경 쓰지 않는 반면, 이진 트리 보너스 과제의 핵심은 **트리 전체를 규칙적으로 방문하는 알고리즘**이며 이건 힙에 전혀 없던 개념이다.

### 순회 4종

노드 구조를 `left`, `right`, `data`로 둔다고 할 때:

```
전위 순회 (Pre-order):  root → left → right   (루트를 먼저 처리 — 트리 복제, 구조 직렬화에 사용)
중위 순회 (In-order):   left → root → right   (BST에서 이 순서로 방문하면 정렬된 결과가 나옴)
후위 순회 (Post-order): left → right → root   (자식을 먼저 처리해야 하는 경우 — 트리 삭제, 디렉토리 용량 계산)
레벨 순회 (Level-order): 루트부터 한 층씩 왼→오로 방문 (BFS와 동일한 방식, 큐를 사용해서 구현)
```

- 전위/중위/후위는 재귀(또는 스택)로 구현하는 DFS 계열이고, 레벨 순회만 **큐**를 사용하는 BFS 계열이다. → 위 2번(스택/큐)에서 만든 자료구조가 여기서 실제로 쓰인다.
- "힙이 완전 이진 트리를 배열로 표현한다"는 관점을 이 과제로 다시 보면: 힙은 인덱스 산술로 트리를 흉내 낸 특수 사례고, 여기서는 포인터 기반 노드로 트리를 표현하며 순회라는 범용 연산을 붙이는 것이 차이점이다.

---

## 4. 이진 탐색 트리 (Binary Search Tree, BST)

### 정의
이진 트리의 한 종류로, 모든 노드에 대해 다음 불변식을 만족한다.

```
왼쪽 서브트리의 모든 값 < 노드 값 < 오른쪽 서브트리의 모든 값
```

### Min-Heap과 무엇이 다른가 (가장 헷갈리기 쉬운 부분)
힙과 BST 둘 다 "이진 트리 + 정렬 관련 규칙"이라 비슷해 보이지만 규칙의 강도가 다르다.

| | Min-Heap | BST |
|---|---|---|
| 정렬 불변식 | 부모 ≤ 자식 (형제간 순서는 규정 없음) | 왼쪽 < 부모 < 오른쪽 (전체적으로 정렬 구조) |
| `peek`/최솟값 찾기 | O(1) (루트가 항상 최솟값) | O(h) — 왼쪽 끝까지 내려가야 함 (h = 트리 높이) |
| 임의의 키 탐색 | 불가능 (O(n) 전체 탐색 필요 — 정렬 규칙이 "최솟값 찾기"만 보장하므로) | **O(h)** — 각 노드에서 좌/우 어느 쪽으로 갈지 바로 결정 가능 |
| 형태 | 항상 완전 이진 트리 (균형 보장) | 삽입 순서에 따라 한쪽으로 치우칠 수 있음 (최악 O(n)) |
| 중위 순회 결과 | 정렬되지 않음 | **항상 정렬된 순서** |

즉 힙은 "최솟값 하나"에 최적화된 구조이고, BST는 "임의의 키로 탐색"에 최적화된 구조다. 이 차이가 왜 두 구조가 별개의 보너스 과제로 나뉘어 있는지에 대한 답이다.

### 핵심 연산

| 메서드 | 로직 | 평균 시간복잡도 | 최악 시간복잡도 |
|---|---|---|---|
| `insert(key)` | 루트부터 key와 비교하며 좌/우로 내려가다 빈 자리에 삽입 | O(log n) | O(n) (치우친 트리) |
| `search(key)` | insert와 같은 방식으로 내려가며 일치하는 노드 탐색 | O(log n) | O(n) |
| `delete(key)` | 리프면 바로 제거, 자식 1개면 그 자식으로 대체, 자식 2개면 **중위 후속자**(오른쪽 서브트리의 최솟값)로 대체 후 그 후속자를 재귀 삭제 | O(log n) | O(n) |
| 중위 순회 | 위 3번의 in-order 그대로 사용 | O(n) | O(n) |

- 평균/최악이 갈리는 이유: 삽입 순서가 무작위면 트리가 대체로 균형을 이뤄 높이가 O(log n)이지만, 정렬된 순서로 계속 삽입하면(1,2,3,4,5...) 한쪽으로만 뻗어나가 사실상 연결 리스트가 되어 O(n)이 된다. (레드-블랙 트리 등 균형 BST는 이 문제를 해결하지만 이번 과제 범위 밖이다.)
- `delete`에서 자식이 2개인 경우가 가장 까다로운 이유: 두 자식을 다 유지하면서도 BST 불변식을 깨지 않는 대체 노드가 필요한데, "오른쪽 서브트리에서 가장 작은 값"이 유일하게 그 조건(왼쪽 서브트리 전체보다 크고, 오른쪽 서브트리 전체보다 작거나 같음)을 만족한다.

### 활용 방향
과제 설명대로 "키 정렬/범위 조회" 확장에 쓰인다 — 예를 들어 Mini Redis에 `ZRANGE` 같은 정렬된 범위 조회 커맨드를 추가한다면, 지금의 해시맵(순서 없음)으로는 불가능하고 BST(또는 정렬 유지 구조)가 필요해진다.

---

## 5. Pub/Sub (발행-구독 메시징)

### 정의
클라이언트가 **채널(channel)**을 구독(`SUBSCRIBE`)해두면, 다른 클라이언트가 그 채널에 메시지를 발행(`PUBLISH`)할 때 구독 중인 모든 클라이언트에게 그 메시지가 전달되는 모델. 발행자는 누가 구독 중인지 몰라도 되고, 구독자는 누가 발행했는지 몰라도 된다 — **발행자와 구독자가 서로를 직접 참조하지 않고 채널을 매개로만 연결**된다는 게 핵심(느슨한 결합, decoupling).

### basic-3-1과 무엇이 다른가 (완전히 새로운 모델)
지금까지 구현한 모든 커맨드(`SET`/`GET`/`DEL`/`EXPIRE`...)는 **"클라이언트가 요청하면 서버가 응답 1번 주고 끝"**인 요청-응답(request-response) 모델이었다. Pub/Sub은 이것과 근본적으로 다르다.

| | 지금까지의 KV 커맨드 | Pub/Sub |
|---|---|---|
| 통신 패턴 | 요청 1개 → 응답 1개 | 발행 1개 → 구독자 N개에게 전달 (1:N) |
| 데이터 지속성 | 값이 저장소에 계속 남음 | 메시지는 저장되지 않고 그 순간 구독 중인 클라이언트에게만 전달(fire-and-forget) |
| 클라이언트 상태 | 무상태(stateless) — 매 요청이 독립적 | 구독 중이라는 **상태**를 서버가 클라이언트별로 계속 들고 있어야 함 |

즉 지금까지 만든 구조는 전부 "데이터를 어떻게 저장/조회하느냐"에 관한 것이었는데, Pub/Sub은 "이벤트를 어떻게 실시간으로 퍼뜨리느냐"에 관한 것이라 성격 자체가 다르다.

### 필요한 상태와 자료구조 재사용 방안

```
channel_subscribers: channel_name -> [클라이언트 커넥션들의 집합/리스트]
```

- **채널 → 구독자 목록** 매핑에 지금 만든 `hash_map.py`를 그대로 재사용할 수 있다 (key = 채널명, value = 구독자 리스트).
- 구독자 리스트/버퍼 자체는 `doubly_linked_list.py`를 **큐(FIFO)로** 재사용할 수 있다 — `PUBLISH`가 오면 각 구독자의 버퍼 뒤(`insert_back`)에 메시지를 쌓고, 클라이언트가 메시지를 소비할 때 앞(`remove_front`)에서 꺼내는 식. 이게 과제 설명의 "구현한 연결 리스트를 메시지 큐로 재활용"이라는 문구의 의미다.

### 핵심 커맨드

| 커맨드 | 동작 |
|---|---|
| `SUBSCRIBE channel` | 현재 클라이언트를 해당 채널의 구독자 목록에 추가 |
| `UNSUBSCRIBE channel` | 구독자 목록에서 제거 |
| `PUBLISH channel message` | 해당 채널의 구독자 전원에게 message를 전달(또는 각자의 큐에 적재), 전달된 구독자 수를 반환 |

### 설계상 주의할 점
- 구독 중인 클라이언트가 끊기면(연결 종료) 모든 채널의 구독자 목록에서 정리(cleanup)해야 한다 — 안 하면 메모리가 계속 쌓인다 (basic-3-1의 `used_memory` eviction과 비슷한 고민이 여기서도 등장).
- `PUBLISH` 시점에 구독자가 0명이면 메시지는 그냥 유실된다 (Redis Pub/Sub 자체가 "저장 안 하는" 모델이라는 특징을 그대로 반영).
